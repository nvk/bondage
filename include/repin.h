#ifndef BONDAGE_REPIN_H
#define BONDAGE_REPIN_H

int
bondage_repin(const char *profile_name, const char *config_path);

int
bondage_repin_globals(const char *config_path);

int
bondage_doctor(const char *config_path);

#endif
